import SwiftUI
import HomeyMusicKit

final class TonicConductor: ViewConductor {
    override init(
        tonalContext: TonalContext
    ) {
        super.init(
            tonalContext: tonalContext
        )
    }
    
}
